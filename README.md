# etl-pipeline
Configurable Python ETL pipeline for automated data processing, integrated with RESTful APIs and a React-based web interface for data visualization and interactive dashboards (work in progress).
